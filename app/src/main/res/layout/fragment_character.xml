<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:gravity="center">

    <LinearLayout
        android:id="@+id/costum_char"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center">

        <ImageView
            android:id="@+id/selectedCharImageView"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:adjustViewBounds="true"
            android:layout_gravity="center"
            android:padding="55dp"
            android:elevation="10dp"
            tools:srcCompat="@drawable/tools_head_front"
            android:background="@drawable/bg_itemframe" />

        <EditText
            android:id="@+id/selectedCharEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="25sp"
            android:gravity="center"
            android:textStyle="bold"
            tools:text="Name"/>

        <Button
            android:id="@+id/selectCharSetButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textColor="@color/primaryTextColor"
            android:background="@color/secondaryColor"
            android:padding="16dp"
            android:layout_marginTop="8dp"
            android:text="@string/set_name" />
    </LinearLayout>
</RelativeLayout>